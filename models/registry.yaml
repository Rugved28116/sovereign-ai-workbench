schema_version: 1
models:
  - id: mock-fast
    provider: mock
    enabled: true
    environments:
      - development
    capabilities:
      - reasoning
      - chat
    context_length: 8192
    priority: 10
    metadata:
      description: Deterministic mock model for fast local development and tests.

  - id: mock-code
    provider: mock
    enabled: true
    environments:
      - development
    capabilities:
      - reasoning
      - coding
    context_length: 16384
    priority: 20
    metadata:
      description: Deterministic mock model for code-oriented development and tests.
